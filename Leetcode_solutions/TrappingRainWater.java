public class TrappingRainWater {
    public int trap(int[] height) {
        int i = 0;
        int j = height.length-1;
        int unitsBetweenThem = 0;
        int maxIndex = -1;
        int[] nextMaxIndex = new int[height.length+1];
        nextMaxIndex[height.length] = 0;

        while(j>0){
            if(j == )
            Math.max(nextMaxIndex[])
        }
        while (j < height.length) {

            if (maxIndex > 0 && height[j] > height[maxIndex]) {
                maxIndex = j;
            }
            

        }
    }
}
